8/17: why are the socials icons not showing up in most of the pages except for projects where they are showing up in blue. Is this because of a styling conflict. Also the size of the footer is unequal when compared to the about page and the other pages.
8/17: read through this article about web components to determine how to pass data: [https://hartenfeller.dev/blog/web-components-step-by-step](https://hartenfeller.dev/blog/web-components-step-by-step)
8/19: stuck on /Users/josephshomefolder/development/courses/html, figuring out why the calculate function does not fire even though the elements seem to be attached to the template
8/22: figuring out how to implement routing so that the urls aren't but ugly and reflect the structure of the files. This may be a larger problem that is not entirely solved by routing, so I may need to look into that before going down the router rabbit hole.
8/23: figured out that I am literally getting every single link on the page. I need to limit it to the links inside of the div with content id.
8/24: I cannot seem to figure out which link, if any are failing. I am under the impression that only a single link is failing the click, but I cannot be sure because I cannot figure out how to actually print the contents of each link. When printing the individual links, even with `.innerText()` I get a promise. 
8/25: read chatgpt thread re: how playwright handles routing
8/27: figure out how to set up an spa with express and then how the history api fits in
8/31: in the middle of redesigning to use the router methodology from the tutorial. I am in conflict, because I want to use web components, whereas the tutorial uses classes in a slightly different way. I should start by understanding what the difference is and then determine how I can adapt that routing method, which is preferable because it encapsulates everything into classes as opposed to using a separate html template, so as to be able to use web components.
9/1: continue to work through debugging getParams and working through [troubleshooting list](https://chatgpt.com/share/6a96d273-b0a8-83ea-af98-8b82c280541c)
9/2: figure out why vite is not bundling the headshot or the the project page. https://scrimba.com/intro-to-vite-c03p6pbbdq/~05pq?via=vite
something
